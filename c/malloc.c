#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {

    foo = null;
    uint *p;
    p++;
    if (a == b) {
    }
    p = malloc((sizeof *p) * 20);
    p++;
    printf("%d", p);
    free(p);

    sqrt(666)

    return 0;
}

